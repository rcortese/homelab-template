# Optional base Compose file containing shared resources and infrastructure plumbing.

x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

x-common-env: &common-env
  TZ: ${TZ}
  PUID: ${PUID}
  PGID: ${PGID}

services: {}

networks:
  public: {}
  internal:
    internal: true

volumes:
  shared_data: {}

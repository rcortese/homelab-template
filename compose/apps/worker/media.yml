services:
  worker:
    container_name: worker-media
    environment:
      WORKER_CONCURRENCY: ${WORKER_MEDIA_CONCURRENCY:-1}
    networks:
      homelab_internal:
        ipv4_address: ${WORKER_NETWORK_IPV4}

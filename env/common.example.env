# Compartilhado • Fuso horário IANA usado em logs e agendamentos (ex.: America/Sao_Paulo).
TZ=America/Sao_Paulo
# Compartilhado • Chave de 24+ caracteres para criptografar dados sensíveis.
APP_SECRET=defina-uma-chave-segura
# Compartilhado • Idade máxima dos registros em horas (padrão 168 = 7 dias).
APP_RETENTION_HOURS=168
# Compartilhado • UID numérico usado para ajustar permissões dos volumes persistentes.
APP_DATA_UID=1000
# Compartilhado • GID numérico usado para ajustar permissões dos volumes persistentes.
APP_DATA_GID=1000
# Derivado pelos scripts • Caminho usado para montar o volume persistente em compose/base.yml (opcional; deixe em branco para usar o mount sanitizado do padrão `data/<app>-<instância>`).
# APP_DATA_DIR_MOUNT=../data/app-core
# Rede interna compartilhada • Nome lógico atribuído à rede de apps no Docker.
APP_NETWORK_NAME=homelab_internal
# Rede interna compartilhada • Driver usado ao criar a rede ("bridge", "macvlan", etc.).
APP_NETWORK_DRIVER=bridge
# Rede interna compartilhada • Sub-rede reservada para os serviços do homelab.
APP_NETWORK_SUBNET=172.20.0.0/24
# Rede interna compartilhada • Gateway disponibilizado aos contêineres (use um IP livre da sub-rede).
APP_NETWORK_GATEWAY=172.20.0.1

# Shared • IANA timezone used in logs and schedules (e.g., America/Sao_Paulo).
TZ=America/Sao_Paulo
# Base app example • 24+ character key to encrypt sensitive data (rename or remove when adapting to the real service).
APP_SECRET=defina-uma-chave-segura
# Base app example • Max record age in hours (default 168 = 7 days). Adjust the prefix to the real identifier or remove if not applicable.
APP_RETENTION_HOURS=168
# Shared • Numeric UID used to align permissions on persistent volumes.
APP_DATA_UID=1000
# Shared • Numeric GID used to align permissions on persistent volumes.
APP_DATA_GID=1000
# Derived by scripts • Logical instance name applied to manifests. Set automatically by the Compose wrappers.
# LOCAL_INSTANCE=core
# Derived by scripts • Default relative directory (`data/<instance>/<app>`). Mutually exclusive with `APP_DATA_DIR_MOUNT`.
# APP_DATA_DIR=data/core/app
# Derived by scripts • Alternative absolute path for the persistent volume. Mutually exclusive with `APP_DATA_DIR`.
# APP_DATA_DIR_MOUNT=/srv/core/app
# Shared internal network • Logical name assigned to the app network in Docker.
APP_NETWORK_NAME=homelab_internal
# Shared internal network • Driver used when creating the network ("bridge", "macvlan", etc.).
APP_NETWORK_DRIVER=bridge
# Shared internal network • Subnet reserved for homelab services.
APP_NETWORK_SUBNET=172.20.0.0/24
# Shared internal network • Gateway exposed to containers (use a free IP from the subnet).
APP_NETWORK_GATEWAY=172.20.0.1
# Shared internal network • Static IP for the app service within the subnet.
APP_NETWORK_IPV4=172.20.0.10
# Shared internal network • Static IP for the monitoring service within the subnet.
MONITORING_NETWORK_IPV4=172.20.0.20
# Shared persistent volume • Docker volume name reused by multiple applications.
APP_SHARED_DATA_VOLUME_NAME=homelab_shared_data
# Example worker • Queue/task processor URL consumed by workers (rename or remove to match the real application worker).
WORKER_QUEUE_URL=redis://queue:6379/0

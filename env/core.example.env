# Obrigatório • Fuso horário IANA usado em logs e agendamentos (ex.: America/Sao_Paulo).
TZ=America/Sao_Paulo
# Obrigatório • Chave de 24+ caracteres para criptografar dados sensíveis.
APP_SECRET=defina-uma-chave-segura
# Obrigatório • Idade máxima dos registros em horas (padrão 168 = 7 dias).
APP_RETENTION_HOURS=168
# Obrigatório ao expor URLs • Hostname base para links e cookies.
APP_PUBLIC_URL=https://app.domain.com
# Obrigatório para webhooks públicos • URL base HTTPS resolvida pelo proxy.
APP_WEBHOOK_URL=https://hooks.domain.com
# Opcional • Porta exposta pelo host para acesso externo.
APP_CORE_PORT=8080
# Opcional • Nome personalizado para o container.
SERVICE_NAME=app-core
# Opcional • Diretório persistente (formato `data/<app>-<instância>`). O padrão é `data/app-core`; altere apenas se o armazenamento viver fora do repositório.
# APP_DATA_DIR=data/app-core
# Opcional • UID numérico usado para ajustar permissões do volume persistente.
APP_DATA_UID=1000
# Opcional • GID numérico usado para ajustar permissões do volume persistente.
APP_DATA_GID=1000

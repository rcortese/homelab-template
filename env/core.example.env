# Este arquivo complementa env/common.example.env. Copie ambos para env/local/.
# Obrigatório ao expor URLs • Hostname base para links e cookies.
APP_PUBLIC_URL=https://app.domain.com
# Obrigatório para webhooks públicos • URL base HTTPS resolvida pelo proxy.
APP_WEBHOOK_URL=https://hooks.domain.com
# Opcional • Porta exposta pelo host para acesso externo.
APP_CORE_PORT=8080
# Derivado pelos scripts • Nome da instância utilizado em diretórios e logs.
# APP_INSTANCE=core
# Derivado pelos scripts • Slug da aplicação principal detectada (ex.: app). Complementa `APP_INSTANCE` no fallback de dados.
# APP_PRIMARY_APP=app
# Opcional • Diretório persistente relativo (`data/<app>-<instância>`). Não combine com `APP_DATA_DIR_MOUNT`.
# APP_DATA_DIR=data/app-core
# Opcional • Caminho absoluto montado quando o armazenamento vive fora do repositório. Não combine com `APP_DATA_DIR`.
# APP_DATA_DIR_MOUNT=/srv/app-core
# Opcional • Override da combinação UID:GID padrão definida em env/common.example.env.
# APP_DATA_UID=1000
# APP_DATA_GID=1000
# Rede interna compartilhada • IP dedicado ao serviço "app" nesta instância.
APP_NETWORK_IPV4=172.20.0.10
# Rede interna compartilhada • IP dedicado ao serviço "monitoring" nesta instância.
MONITORING_NETWORK_IPV4=172.20.0.20
# Opcional • Ajusta a concorrência do worker nesta instância.
# WORKER_CORE_CONCURRENCY=2
# Rede interna compartilhada • IP dedicado ao serviço "worker" nesta instância.
WORKER_CORE_NETWORK_IPV4=172.20.0.30
# Rede externa (ex.: proxy reverso) dedicada à instância core.
CORE_PROXY_NETWORK_NAME=traefik_proxy
# IP atribuído ao container principal dentro da rede externa acima.
CORE_PROXY_IPV4=172.21.0.10
# Volume nomeado para logs persistentes compartilhados entre serviços da instância.
CORE_LOGS_VOLUME_NAME=core_logs

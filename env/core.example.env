# Este arquivo complementa env/common.example.env. Copie ambos para env/local/.
# Aviso: os nomes prefixados com `APP_` usam o slug "app" apenas como ilustração; substitua pelo identificador real (ex.: `API`, `FRONTEND`) ao criar o fork.
# Obrigatório ao expor URLs • Hostname base para links e cookies. (Use `API_PUBLIC_URL`, `FRONTEND_PUBLIC_URL`, etc., conforme o slug real.)
APP_PUBLIC_URL=https://app.domain.com
# Obrigatório para webhooks públicos • URL base HTTPS resolvida pelo proxy. Renomeie o prefixo `APP_` para refletir o serviço verdadeiro.
APP_WEBHOOK_URL=https://hooks.domain.com
# Opcional • Porta exposta pelo host para acesso externo. Ajuste o prefixo `APP_` conforme o nome real do serviço.
APP_CORE_PORT=8080
# Opcional • Diretório persistente relativo (`data/<instância>/<app>`). Não combine com `APP_DATA_DIR_MOUNT`. Troque `APP_` pelo slug da aplicação real.
# APP_DATA_DIR=data/core/app
# Opcional • Caminho absoluto montado quando o armazenamento vive fora do repositório. Não combine com `APP_DATA_DIR`. Renomeie o prefixo `APP_` conforme apropriado.
# APP_DATA_DIR_MOUNT=/srv/core/app
# Opcional • Override da combinação UID:GID padrão definida em env/common.example.env. Ajuste o prefixo `APP_` para combinar com o serviço real.
# APP_DATA_UID=1000
# APP_DATA_GID=1000
# Rede interna compartilhada • IP dedicado ao serviço "app" nesta instância. Substitua `APP_` pelo slug da aplicação configurada.
APP_NETWORK_IPV4=172.20.0.10
# Rede interna compartilhada • IP dedicado ao serviço "monitoring" nesta instância.
MONITORING_NETWORK_IPV4=172.20.0.20
# Opcional • Ajusta a concorrência do worker nesta instância.
# WORKER_CORE_CONCURRENCY=2
# Rede interna compartilhada • IP dedicado ao serviço "worker" nesta instância.
WORKER_CORE_NETWORK_IPV4=172.20.0.30
# Rede externa (ex.: proxy reverso) dedicada à instância core.
CORE_PROXY_NETWORK_NAME=traefik_proxy
# IP atribuído ao container principal dentro da rede externa acima.
CORE_PROXY_IPV4=172.21.0.10
# Volume nomeado para logs persistentes compartilhados entre serviços da instância.
CORE_LOGS_VOLUME_NAME=core_logs

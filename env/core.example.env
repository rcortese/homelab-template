# Este arquivo complementa env/common.example.env. Copie ambos para env/local/.
# Obrigatório ao expor URLs • Hostname base para links e cookies.
APP_PUBLIC_URL=https://app.domain.com
# Obrigatório para webhooks públicos • URL base HTTPS resolvida pelo proxy.
APP_WEBHOOK_URL=https://hooks.domain.com
# Opcional • Porta exposta pelo host para acesso externo.
APP_CORE_PORT=8080
# Opcional • Nome personalizado para o container.
SERVICE_NAME=app-core
# Opcional • Diretório persistente (formato `data/<app>-<instância>`). Altere apenas se o armazenamento viver fora do repositório.
# APP_DATA_DIR=data/app-core
# Opcional • Override da combinação UID:GID padrão definida em env/common.example.env.
# APP_DATA_UID=1000
# APP_DATA_GID=1000
# Rede interna compartilhada • IP dedicado ao serviço "app" nesta instância.
APP_NETWORK_IPV4=172.20.0.10
# Rede interna compartilhada • IP dedicado ao serviço "monitoring" nesta instância.
MONITORING_NETWORK_IPV4=172.20.0.20

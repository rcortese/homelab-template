# This file complements env/common.example.env. Copy both to env/local/.
# Warning: names prefixed with `APP_` use the "app" slug only as an illustration; replace it with the real identifier (e.g., `API`, `FRONTEND`) when creating the fork.
# The repository root is derived automatically by scripts; the generated root .env
# includes REPO_ROOT when needed.
# Required when exposing URLs • Base hostname for links and cookies. (Use `API_PUBLIC_URL`, `FRONTEND_PUBLIC_URL`, etc., per the real slug.)
APP_PUBLIC_URL=https://app.domain.com
# Required for public webhooks • HTTPS base URL resolved by the proxy. Rename the `APP_` prefix to reflect the real service.
APP_WEBHOOK_URL=https://hooks.domain.com
# Optional • Host-exposed port for external access. Adjust the `APP_` prefix to match the real service name.
APP_CORE_PORT=8080
# Optional • Override for the default UID:GID pair defined in env/common.example.env. Adjust the `APP_` prefix to match the real service.
# APP_DATA_UID=1000
# APP_DATA_GID=1000
# Shared internal network • Dedicated IP for the "app" service in this instance. Replace `APP_` with the configured application slug.
APP_NETWORK_IPV4=172.20.0.10
# Shared internal network • Dedicated IP for the "monitoring" service in this instance.
MONITORING_NETWORK_IPV4=172.20.0.20
# Optional • Adjusts worker concurrency in this instance.
# WORKER_CORE_CONCURRENCY=2
# Shared internal network • Dedicated IP for the "worker" service in this instance.
WORKER_CORE_NETWORK_IPV4=172.20.0.30
# External network (e.g., reverse proxy) dedicated to the core instance.
CORE_PROXY_NETWORK_NAME=traefik_proxy
# IP assigned to the main container within the external network above.
CORE_PROXY_IPV4=172.21.0.10
# Named volume for persistent logs shared among instance services.
CORE_LOGS_VOLUME_NAME=core_logs

# Obrigatório • Fuso horário IANA usado em logs e agendamentos (ex.: America/Sao_Paulo).
TZ=America/Sao_Paulo
# Obrigatório • Mesma chave de 24+ caracteres da instância core para criptografar dados.
APP_SECRET=defina-uma-chave-segura
# Obrigatório • Idade máxima dos registros em horas (padrão 168 = 7 dias).
APP_RETENTION_HOURS=168
# Obrigatório ao montar workloads de mídia • Caminho no host exportado para o container.
MEDIA_HOST_PATH=/mnt/data
# Opcional • Porta exposta pelo host para acesso externo.
APP_MEDIA_PORT=8081
# Opcional • Nome personalizado para o container.
SERVICE_NAME=app-media
# Opcional • Diretório usado para persistência do serviço.
APP_DATA_DIR=data
# Opcional • UID numérico usado para ajustar permissões do volume persistente.
APP_DATA_UID=1000
# Opcional • GID numérico usado para ajustar permissões do volume persistente.
APP_DATA_GID=1000

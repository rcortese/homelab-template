# This file complements env/common.example.env. Copy both to env/local/.
# Warning: variables prefixed with `APP_` use the "app" slug only as an example; replace it with the real service identifier (e.g., `MEDIA`, `PLEX`) when creating the fork.
# The repository root is derived automatically by scripts; the generated root .env
# includes REPO_ROOT when needed.
# Required when mounting media workloads • Host path exported to the container.
MEDIA_HOST_PATH=/mnt/data
# Optional • Host-exposed port for external access. Rename `APP_MEDIA_PORT` to reflect the configured application prefix.
APP_MEDIA_PORT=8081
# Optional • Override for the default UID:GID pair defined in env/common.example.env. Update the `APP_` prefix to match the real service.
# APP_DATA_UID=1000
# APP_DATA_GID=1000
# Shared internal network • Dedicated IP for the "app" service in this instance (different from other instances). Use the real slug prefix when defining `*_NETWORK_IPV4`.
APP_NETWORK_IPV4=172.20.0.11
# IP reserved for the monitoring service when enabled in this instance.
MONITORING_NETWORK_IPV4=172.20.0.21
# Optional • Adjusts worker concurrency in this instance.
# WORKER_MEDIA_CONCURRENCY=1
# Shared internal network • Dedicated IP for the "worker" service in this instance.
WORKER_MEDIA_NETWORK_IPV4=172.20.0.31
# Named volume for transcoding caches shared among media services.
MEDIA_CACHE_VOLUME_NAME=media_cache

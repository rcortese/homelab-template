# Este arquivo complementa env/common.example.env. Copie ambos para env/local/.
# Obrigatório ao montar workloads de mídia • Caminho no host exportado para o container.
MEDIA_HOST_PATH=/mnt/data
# Opcional • Porta exposta pelo host para acesso externo.
APP_MEDIA_PORT=8081
# Opcional • Nome personalizado para o container.
SERVICE_NAME=app-media
# Opcional • Diretório persistente (formato `data/<app>-<instância>`). O padrão é `data/app-media`; altere apenas se o armazenamento viver fora do repositório.
# APP_DATA_DIR=data/app-media
# Opcional • Override da combinação UID:GID padrão definida em env/common.example.env.
# APP_DATA_UID=1000
# APP_DATA_GID=1000
